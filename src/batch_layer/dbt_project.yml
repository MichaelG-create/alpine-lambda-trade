name: 'alpine_lambda_trade'
version: '1.0.0'
config-version: 2

profile: 'alpine_lambda_trade'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  alpine_lambda_trade:
    staging:
      +materialized: view
      +schema: STAGING
    silver:
      +materialized: incremental
      +schema: SILVER
    serving:
      +materialized: view
      +schema: SERVING
